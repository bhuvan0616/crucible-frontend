---
phase: 04-ui-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/orders/OrderListClient.tsx
  - components/orders/OrderDetailClient.tsx
  - components/orders/StatusBadge.tsx
autonomous: true
requirements:
  - ORDR-12
  - ORDR-13
  - ORDR-14
---

<objective>
Polish UI for Order History pages: improve skeleton loading states with shimmer animations, ensure mobile-first responsive design, and verify design system consistency. Deliver polished order list and order detail pages.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-status-display/03-01-SUMMARY.md

Design System (from PRD):
- Primary: #0F172A (Deep Slate)
- Accent: #14B8A6 (Teal)
- CTA: #F97316 (Orange)
- Dark mode default
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade skeleton loading with shimmer animation</name>
  <files>components/orders/OrderListClient.tsx, components/orders/OrderDetailClient.tsx</files>
  <action>
Improve skeleton loading states by adding shimmer (gradient sweep) animation instead of basic pulse.

**OrderListClient.tsx skeleton improvements:**
- Add CSS keyframe shimmer animation to globals.css (if not present) or inline via Tailwind Animate:
  - `bg-gradient-to-r from-muted via-muted/50 to-muted` with `bg-[length:200%_100%] animate-[shimmer_east_2s_infinite]`
- Skeleton cards should mirror actual card layout: order number line, date line, price line, status badge placeholder
- Add 5 skeleton cards for order list (not just 3) to show pagination expectation

**OrderDetailClient.tsx skeleton improvements:**
- Create skeleton placeholders that mirror actual page sections: order header card, timeline card, line items table, order totals card, address/payment cards
- Add shimmer to all skeleton elements
- Ensure vertical layout flows in correct order during loading

Do NOT change the loading state logic (keep existing isLoading/isError/isAuthenticated checks).
  </action>
  <verify>grep -c "shimmer\|animate-pulse" components/orders/OrderListClient.tsx && grep -c "shimmer\|animate-pulse" components/orders/OrderDetailClient.tsx && echo "both files updated"</verify>
  <done>All skeleton elements use shimmer animation (gradient sweep), skeleton cards mirror actual content layout order</done>
</task>

<task type="auto">
  <name>Task 2: Fix responsive layout for mobile-first</name>
  <files>components/orders/OrderListClient.tsx, components/orders/OrderDetailClient.tsx</files>
  <action>
Ensure mobile-first responsive design across both order pages.

**OrderListClient.tsx:**
- Tab filter scroll on mobile: `overflow-x-auto` is already set — verify no horizontal overflow issues
- Order cards: already flex column layout — verify spacing works on 320px screens
- Search input: full width already — verify touch target size (min 44px height)

**OrderDetailClient.tsx:**
- Order header card: `flex-wrap justify-between` already set — verify alignment on small screens
- Line items table: add `overflow-x-auto` wrapper if not present (table must scroll horizontally on mobile < 640px)
- Add responsive line clamp to product names if they overflow: `line-clamp-2`
- Two-column grid for address/payment: `grid-cols-1 md:grid-cols-2` is correct — verify gap and padding on mobile
- Pagination controls (if added later): ensure touch-friendly button sizes

**OrderTimeline.tsx:**
- Timeline only has desktop horizontal view per Phase 3 — no mobile view implemented. Add vertical mobile layout: show active stage prominently with "completed" stages listed below as text, "pending" stages grayed below.
- Use `hidden md:flex` for horizontal desktop and `flex md:hidden flex-col` for mobile vertical layout.
  </action>
  <verify>grep -c "overflow-x-auto" components/orders/OrderDetailClient.tsx && grep -c "md:grid-cols-2" components/orders/OrderDetailClient.tsx && echo "responsive in place"</verify>
  <done>Order pages responsive at sm:640px, md:768px, lg:1024px breakpoints</done>
</task>

<task type="auto">
  <name>Task 3: Verify and fix design system consistency</name>
  <files>components/orders/StatusBadge.tsx, components/orders/OrderListClient.tsx, components/orders/OrderDetailClient.tsx</files>
  <action>
Verify design system consistency and fix any issues.

**StatusBadge.tsx color audit:**
- Status colors currently use light mode classes (`bg-amber-100 text-amber-800`). For dark mode default, review and switch to dark-mode-aware variants or keep light-mode since status badges are typically on cards which have their own background.
- If badge background conflicts with dark card backgrounds, use dark-mode classes: `dark:bg-amber-900/30 dark:text-amber-400 dark:border-amber-500/30`
- Badge colors should be consistent with brand: amber=pending, blue=processing, purple=shipped, green=delivered, red=cancelled

**Spacing and typography consistency:**
- Verify all order cards use same Card component variants
- Verify all text hierarchy matches: CardTitle (text-base or text-xl), CardDescription (mt-1 text-muted-foreground)
- Verify all margins/padding follow 4px base unit (use Tailwind spacing scale: 4, 6, 8 = gap-1, gap-2, gap-3)

**Empty and error state consistency:**
- Empty state already uses teal gradient button (correct per brand)
- Error state uses red text + primary button (correct)
- Verify spacing is consistent between both states (py-16, centered, mb-4)

Do NOT change component structure or add new components — only fix colors and spacing.
  </action>
  <verify>grep -c "dark:" components/orders/StatusBadge.tsx && echo "dark-mode aware badges verified"</verify>
  <done>All status badges use dark-mode-compatible classes, all spacing follows Tailwind 4px scale</done>
</task>

</tasks>

<success_criteria>
1. OrderListClient shows 5 shimmer skeleton cards during loading, mirroring card layout
2. PurchaseDetailClient shows all section skeletons with shimmer during loading
3. OrderTimeline shows vertical layout on mobile, horizontal on desktop
4. Price formatting is consistent throughout, all spacing uses 4px grid
5. All components use dark-mode-aware colors via Tailwind dark: classes
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish/04-01-SUMMARY.md`
</output>

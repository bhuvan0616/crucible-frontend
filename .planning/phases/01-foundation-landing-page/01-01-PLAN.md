---
phase: 01-foundation-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - tailwind.config.ts
  - next.config.ts
  - mocks/products.json
  - package.json
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "Design system colors are defined as CSS variables"
    - "Dark mode is default across the application"
    - "Mock product data follows MedusaJS-compatible structure"
    - "shadcn/ui components are installed and configured"
    - "Product images are referenced correctly"
  artifacts:
    - path: "app/globals.css"
      provides: "CSS variables for brand colors, dark mode base"
      min_lines: 40
    - path: "mocks/products.json"
      provides: "MedusaJS-compatible product data with 3 variants, prices in paise"
      exports: "products array with variants, options, images, metadata"
    - path: "components/ui/button.tsx"
      provides: "shadcn/ui button component"
    - path: "tailwind.config.ts"
      provides: "Brand color extensions (slate, teal, orange)"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import ./globals.css"
    - from: "globals.css"
      to: "tailwind"
      via: "@import tailwindcss + @theme inline"
---

<objective>
Set up the design system foundation with brand colors, dark mode default, shadcn/ui components, and MedusaJS-compatible mock product data.
</objective>

<execution_context>
@/home/bhuvanesh/Workspace/NextProjects/crucible/app/globals.css
@/home/bhuvanesh/Workspace/NextProjects/crucible/app/layout.tsx
@/home/bhuvanesh/Workspace/NextProjects/crucible/crucible-creations-prd.md (Section 3: Design System, Section 6: Mock Data)
</execution_context>

<context>
## Design System Requirements

Brand colors from PRD:
- Primary: `#0F172A` (Deep Slate)
- Accent: `#14B8A6` (Teal)
- CTA: `#F97316` (Orange)
- Neutral Mid: `#64748B`
- Neutral Light: `#F1F5F9`
- White: `#FFFFFF`

Dark mode default, light mode supported via Tailwind `dark:` classes.

## Mock Data Structure (ARCH-01)

Products.json must follow MedusaJS v2 schema:
- Prices in paise (44900 = ₹449.00)
- 3 variants: Wakanda Black, Batman Grey, Captain Teal
- Options: Edition (color)
- Images array with ids and URLs
- Metadata for dimensions, material, customization settings

## shadcn/ui Setup

Using Tailwind CSS 4 + shadcn/ui. Need to:
1. Initialize shadcn/ui with dark mode default
2. Install required components (button, card, input, etc.)
3. Configure components/ui with brand colors

## Project Directory Structure

```
crucible/
├── app/
│   ├── layout.tsx       # Root layout with font setup
│   ├── page.tsx         # Landing page (to be built in Plan 02)
│   ├── globals.css      # CSS variables + Tailwind
├── components/
│   ├── ui/              # shadcn primitives
│   ├── product/         # Product-related components
│   ├── cart/            # Cart components
│   └── layout/          # Navbar, Footer
├── mocks/
│   └── products.json    # MedusaJS-compatible mock data
├── lib/
│   └── services/        # productService.ts, cartService.ts (Phase 2)
└── store/
    └── cartStore.ts     # Zustand (Phase 2)
```

## Image References

Product images are in `/images/` folder at project root. Reference from there in mock data.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind brand colors and CSS variables</name>
  <files>app/globals.css, tailwind.config.ts</files>
  <action>
Create `tailwind.config.ts` in project root with brand color extensions:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        slate: {
          DEFAULT: "#0F172A",
          50: "#F1F5F9",
          100: "#E2E8F0",
          200: "#CBD5E1",
          300: "#94A3B8",
          400: "#64748B",
          500: "#475569",
          600: "#334155",
          700: "#1E293B",
          800: "#0F172A",
          900: "#020617",
        },
        teal: {
          DEFAULT: "#14B8A6",
          50: "#F0FDFA",
          100: "#CCFBF1",
          200: "#99F6E4",
          300: "#5EEAD4",
          400: "#2DD4BF",
          500: "#14B8A6",
          600: "#0D9488",
          700: "#0F766E",
        },
        orange: {
          DEFAULT: "#F97316",
          50: "#FFF7ED",
          100: "#FFEDD5",
          200: "#FED7AA",
          300: "#FDBA74",
          400: "#FB923C",
          500: "#F97316",
          600: "#EA580C",
          700: "#C2410C",
        },
      },
    },
  },
};

export default config;
```

Update `app/globals.css` with brand CSS variables and dark mode as default:

```css
@import "tailwindcss";

:root {
  /* Brand colors */
  --color-primary: #0F172A;
  --color-accent: #14B8A6;
  --color-cta: #F97316;
  --color-neutral-mid: #64748B;
  --color-neutral-light: #F1F5F9;
  --color-white: #FFFFFF;
  
  /* Light mode vars */
  --background: var(--color-white);
  --foreground: var(--color-primary);
  --card-bg: #F1F5F9;
  --card-foreground: var(--color-primary);
  --muted: #E2E8F0;
  --muted-foreground: var(--color-neutral-mid);
  --border: #CBD5E1;
}

.dark {
  /* Dark mode vars - default */
  --background: var(--color-primary);
  --foreground: var(--color-white);
  --card-bg: #1E293B;
  --card-foreground: var(--color-white);
  --muted: #334155;
  --muted-foreground: #94A3B8;
  --border: #475569;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card-bg);
  --color-card-foreground: var(--card-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-border: var(--border);
  --color-accent: var(--color-accent);
  --color-cta: var(--color-cta);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

body {
  background: var(--background);
  color: var(--foreground);
  font-family: var(--font-sans), Arial, sans-serif;
}
```

NOTE: Since project uses Tailwind CSS 4, the config format may differ. Use CSS variables approach for maximum compatibility with Tailwind 4's @theme directive.
  </action>
  <verify>
    <automated>grep -c "0F172A\|14B8A6\|F97316" app/globals.css</automated>
  </verify>
  <done>Tailwind config has brand colors (slate, teal, orange), dark mode class strategy enabled, CSS variables match PRD palette</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn/ui components</name>
  <files>package.json, components/ui/*.tsx</files>
  <action>
Install shadcn/ui components using the CLI. Use npx to initialize:

```bash
cd /home/bhuvanesh/Workspace/NextProjects/crucible
npx shadcn@latest init --defaults --force
```

This will:
- Set up components.json with dark mode class strategy
- Configure the components directory
- Install base components

Then add the components needed for Phase 1:

```bash
npx shadcn@latest add button card input label -y
```

Components to add:
- button.tsx (CTA buttons)
- card.tsx (product cards, testimonials)
- input.tsx (newsletter email)
- label.tsx (form labels)

After installation, verify the components/ui directory exists with the component files.
  </action>
  <verify>
    <automated>ls components/ui/button.tsx components/ui/card.tsx components/ui/input.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>shadcn/ui button, card, input, label components installed in components/ui/</done>
</task>

<task type="auto">
  <name>Task 3: Create MedusaJS-compatible mock product data</name>
  <files>mocks/products.json</files>
  <action>
Create mocks/ directory and products.json with MedusaJS-compatible structure:

```json
{
  "products": [
    {
      "id": "prod_01keychain_phone_stand",
      "title": "Portable Keychain Phone Stand",
      "subtitle": "Foldable • Compact • Customizable",
      "description": "The ultimate portable phone stand that doubles as a keychain. Engineered for everyday carry with a premium 3D-printed finish.",
      "handle": "portable-keychain-phone-stand",
      "status": "published",
      "thumbnail": "/images/products/keychain-stand-thumbnail.jpg",
      "images": [
        { "id": "img_01", "url": "/images/keychain-stand-front.jpg" },
        { "id": "img_02", "url": "/images/keychain-stand-side.jpg" },
        { "id": "img_03", "url": "/images/keychain-stand-lifestyle.jpg" }
      ],
      "options": [
        {
          "id": "opt_color",
          "title": "Edition",
          "product_id": "prod_01keychain_phone_stand",
          "values": [
            { "id": "optval_black", "value": "Wakanda Black", "option_id": "opt_color" },
            { "id": "optval_grey", "value": "Batman Grey", "option_id": "opt_color" },
            { "id": "optval_teal", "value": "Captain Teal", "option_id": "opt_color" }
          ]
        }
      ],
      "variants": [
        {
          "id": "var_black",
          "title": "Wakanda Black",
          "sku": "PKS-BLK-001",
          "manage_inventory": true,
          "inventory_quantity": 100,
          "allow_backorder": false,
          "prices": [
            { "id": "price_blk_inr", "amount": 44900, "currency_code": "inr" }
          ],
          "options": [
            { "option_id": "opt_color", "value": "Wakanda Black" }
          ]
        },
        {
          "id": "var_grey",
          "title": "Batman Grey",
          "sku": "PKS-GRY-002",
          "manage_inventory": true,
          "inventory_quantity": 100,
          "allow_backorder": false,
          "prices": [
            { "id": "price_gry_inr", "amount": 44900, "currency_code": "inr" }
          ],
          "options": [
            { "option_id": "opt_color", "value": "Batman Grey" }
          ]
        },
        {
          "id": "var_teal",
          "title": "Captain Teal",
          "sku": "PKS-TEL-003",
          "manage_inventory": true,
          "inventory_quantity": 100,
          "allow_backorder": false,
          "prices": [
            { "id": "price_tel_inr", "amount": 44900, "currency_code": "inr" }
          ],
          "options": [
            { "option_id": "opt_color", "value": "Captain Teal" }
          ]
        }
      ],
      "metadata": {
        "dimensions": "73mm x 35mm x 12mm",
        "material": "Premium PLA / PETG",
        "customization": true,
        "max_chars": 12,
        "customization_label": "Name / Text to Engrave",
        "customization_helper": "This text will be 3D printed on your stand"
      }
    }
  ]
}
```

Price amount is 44900 = ₹449.00 (paise representation per MedusaJS convention).

Verify mocks directory exists and file is valid JSON.
  </action>
  <verify>
    <automated>cat mocks/products.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'{len(d[\"products\"])} product(s), {len(d[\"products\"][0][\"variants\"])} variants')"</automated>
  </verify>
  <done>products.json exists with 1 product, 3 variants, correct MedusaJS structure, prices in paise</done>
</task>

<task type="auto">
  <name>Task 4: Create SKELETON.md architectural record</name>
  <files>.planning/phases/01-foundation-landing-page/SKELETON.md</files>
  <action>
Create SKELETON.md in the phase directory to record architectural decisions for subsequent phases:

```markdown
# Walking Skeleton — Crucible Creations Storefront

**Phase:** 1
**Generated:** 2026-05-16

## Capability Proven End-to-End

A user can visit the landing page and see the hero section with the product, navigate to shop, view product details, and see the site is fully functional and styled with the brand design system.

## Architectural Decisions

| Decision | Choice | Rationale |
|---|---|---|
| Framework | Next.js 16 (App Router) | App Router, TypeScript, latest stable |
| Styling | Tailwind CSS 4 + CSS variables | Brand colors via CSS vars, dark mode via .dark class |
| UI Components | shadcn/ui | Accessible, composable, dark mode ready |
| Animations | Framer Motion | Subtle, performant animations |
| State Management | Zustand (Phase 2) | Lightweight, TypeScript-friendly |
| Data Layer | MedusaJS-compatible mock (Phase 2 SDK swap) | prices in paise, variant structure mirrors MedusaJS v2 |
| Analytics | GA4 (Phase 3) | afterInteractive strategy |

## Stack Touched in Phase 1

- [x] Project scaffold (existing Next.js 16 + Tailwind 4 + Framer Motion)
- [x] Design system (brand colors, dark mode, CSS variables)
- [x] Routing — landing page at app/page.tsx
- [x] UI components — shadcn/ui button, card, input, label
- [x] Mock data — MedusaJS-compatible products.json
- [ ] Landing page — all sections (hero, benefits, testimonials, newsletter)

## Out of Scope (Deferred to Later Slices)

- Zustand cart store (Phase 2)
- Product gallery / PDP (Phase 2)
- Cart page (Phase 3)
- Checkout flow (Phase 3)
- GA4 analytics (Phase 3)
- Live MedusaJS backend (Phase 2 SDK swap)
- Real payment processing (future)

## Subsequent Slice Plan

- Phase 2: Product Gallery & PDP — full browsing and customization experience
- Phase 3: Cart, Checkout & Analytics — purchase flow and tracking
```
  </action>
  <verify>
    <automated>cat .planning/phases/01-foundation-landing-page/SKELETON.md | head -20</automated>
  </verify>
  <done>SKELETON.md exists with all architectural decisions documented</done>
</task>

</tasks>

<verification>
- Tailwind config has slate, teal, orange color extensions
- app/globals.css has --color-primary: #0F172A, --color-accent: #14B8A6, --color-cta: #F97316
- Dark mode uses .dark class strategy
- shadcn/ui components exist in components/ui/
- mocks/products.json has 1 product with 3 variants
- All prices in paise (44900 = ₹449.00)
- SKELETON.md records architectural decisions
</verification>

<success_criteria>
- Tailwind configured with brand colors from PRD
- CSS variables set for dark mode default
- shadcn/ui components installed (button, card, input, label)
- Mock products.json follows MedusaJS v2 schema
- Prices stored as paise (44900 for ₹449)
- SKELETON.md created for subsequent phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-landing-page/01-01-SUMMARY.md`
</output>
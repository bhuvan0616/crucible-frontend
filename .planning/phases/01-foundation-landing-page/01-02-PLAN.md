---
phase: 01-foundation-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/layout/navbar.tsx
  - components/layout/footer.tsx
  - components/landing/hero.tsx
  - components/landing/featured-products.tsx
  - components/landing/benefits.tsx
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
must_haves:
  truths:
    - "User can see hero banner with lifestyle imagery"
    - "User can click CTA button to navigate to shop"
    - "User can see 3 product variants displayed"
    - "User can see benefit cards explaining product value"
    - "Page loads with dark mode styling by default"
  artifacts:
    - path: "components/landing/hero.tsx"
      provides: "Hero banner with lifestyle image, headline, CTA button"
    - path: "components/landing/featured-products.tsx"
      provides: "3 variant cards showing product images, names, prices"
    - path: "components/landing/benefits.tsx"
      provides: "4 benefit cards with icons and descriptions"
    - path: "components/layout/navbar.tsx"
      provides: "Navigation bar with logo and links"
    - path: "components/layout/footer.tsx"
      provides: "Footer with brand info"
    - path: "app/page.tsx"
      provides: "Landing page assembling all sections"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/hero.tsx"
      via: "import Hero from components/landing/hero"
    - from: "components/landing/hero.tsx"
      to: "components/ui/button.tsx"
      via: "import { Button } from components/ui/button"
---

<objective>
Build the landing page with Hero, Featured Products, and Benefits sections — creating the main visual entry point and product showcase.
</objective>

<execution_context>
@/home/bhuvanesh/Workspace/NextProjects/crucible/app/page.tsx
@/home/bhuvanesh/Workspace/NextProjects/crucible/crucible-creations-prd.md (Section 4.1: Landing Page)
@/home/bhuvanesh/Workspace/NextProjects/crucible/mocks/products.json
</execution_context>

<context>
## Phase 1 User Story

**As a** visitor, **I want to** see a premium landing page with the product and its benefits, **so that** I understand what Crucible Creations offers and feel confident exploring the shop.

## Requirements for This Plan

**LAND-01: Hero section with lifestyle imagery and prominent CTA**
- Full-width hero with background image
- Headline: "Portable Keychain Phone Stand"
- Subheadline: "Foldable • Compact • Customizable"
- CTA button: "Shop Now" → links to /shop
- Dark overlay on image for text readability

**LAND-02: Featured product showcase (3 variants)**
- 3-card grid showing: Wakanda Black, Batman Grey, Captain Teal
- Each card shows: product image, variant name, price (₹449)
- Click navigates to product detail

**LAND-03: Benefits section highlighting product value**
- 4 benefit cards with icons
- Benefits: "Premium 3D Printed", "Compact & Portable", "Custom Engraving", "Perfect Gift"
- Icon + title + description format

## Image Locations

Product images are in `/images/` folder. Use these references:
- Hero lifestyle: `/images/hero-lifestyle.jpg` (from PRD)
- Product variants: `/images/keychain-stand-front.jpg`, `/images/keychain-stand-side.jpg`, `/images/keychain-stand-lifestyle.jpg`

## Component Structure

```
app/
└── page.tsx              # Landing page - assembles all sections

components/
├── landing/
│   ├── hero.tsx          # Hero section with background image
│   ├── featured-products.tsx  # 3-variant product showcase
│   └── benefits.tsx      # 4 benefit cards
├── layout/
│   ├── navbar.tsx        # Top navigation
│   └── footer.tsx        # Bottom footer
└── ui/
    ├── button.tsx        # shadcn button
    └── card.tsx          # shadcn card
```

## Design System

Use CSS variables from globals.css:
- `--color-primary`: #0F172A (Deep Slate)
- `--color-accent`: #14B8A6 (Teal)
- `--color-cta`: #F97316 (Orange)
- Text on dark: white (#FFFFFF)
- Use dark: classes for dark mode default

## Framer Motion Animations

Add subtle animations for premium feel:
- Hero: fade-in on load, subtle parallax on scroll
- Cards: stagger animation on mount
- Use motion from framer-motion
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layout components (Navbar, Footer)</name>
  <files>components/layout/navbar.tsx, components/layout/footer.tsx</files>
  <action>
Create the navigation bar and footer layout components first, as they frame the page.

**Navbar (`components/layout/navbar.tsx`):**
```tsx
"use client";

import Link from "next/link";
import { motion } from "framer-motion";

export function Navbar() {
  return (
    <nav className="fixed top-0 left-0 right-0 z-50 bg-[var(--color-primary)]/90 backdrop-blur-sm border-b border-[var(--border)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">
          <Link href="/" className="flex items-center gap-2">
            <span className="text-xl font-bold text-[var(--color-accent)]">Crucible</span>
            <span className="text-lg text-white">Creations</span>
          </Link>
          
          <div className="flex items-center gap-6">
            <Link href="/" className="text-white hover:text-[var(--color-accent)] transition-colors">
              Home
            </Link>
            <Link href="/shop" className="text-white hover:text-[var(--color-accent)] transition-colors">
              Shop
            </Link>
            <Link 
              href="/shop" 
              className="px-4 py-2 rounded-full bg-[var(--color-cta)] text-white font-medium hover:bg-[var(--color-cta)]/90 transition-colors"
            >
              Shop Now
            </Link>
          </div>
        </div>
      </div>
    </nav>
  );
}
```

**Footer (`components/layout/footer.tsx`):**
```tsx
import Link from "next/link";

export function Footer() {
  return (
    <footer className="bg-[var(--color-primary)] border-t border-[var(--border)] py-12">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex flex-col md:flex-row items-center justify-between gap-6">
          <div className="text-center md:text-left">
            <span className="text-xl font-bold text-[var(--color-accent)]">Crucible Creations</span>
            <p className="text-[var(--muted-foreground)] mt-2">Premium 3D Printed Products</p>
          </div>
          
          <div className="flex items-center gap-6 text-sm text-[var(--muted-foreground)]">
            <Link href="/" className="hover:text-white transition-colors">Home</Link>
            <Link href="/shop" className="hover:text-white transition-colors">Shop</Link>
            <span>© 2026 Crucible Creations</span>
          </div>
        </div>
      </div>
    </footer>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/layout/navbar.tsx components/layout/footer.tsx 2>/dev/null && echo "Files exist"</automated>
  </verify>
  <done>Navbar with logo and Shop Now CTA, Footer with brand name</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section with lifestyle imagery and CTA</name>
  <files>components/landing/hero.tsx, app/page.tsx</files>
  <action>
Create the Hero section component with framer-motion animation:

**`components/landing/hero.tsx`:**
```tsx
"use client";

import { motion } from "framer-motion";
import Link from "next/link";
import { Button } from "@/components/ui/button";

export function Hero() {
  return (
    <section className="relative min-h-screen flex items-center justify-center overflow-hidden bg-[var(--color-primary)]">
      {/* Background image placeholder - use a gradient for now */}
      <div 
        className="absolute inset-0 bg-gradient-to-br from-[#0F172A] via-[#1E293B] to-[#0F172A]"
        style={{
          backgroundImage: `url('/images/hero-lifestyle.jpg')`,
          backgroundSize: "cover",
          backgroundPosition: "center",
          opacity: 0.3,
        }}
      />
      
      {/* Dark overlay */}
      <div className="absolute inset-0 bg-gradient-to-t from-[var(--color-primary)] via-transparent to-[var(--color-primary)]" />
      
      {/* Content */}
      <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, ease: "easeOut" }}
          className="space-y-8"
        >
          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.2, duration: 0.6 }}
            className="text-[var(--color-accent)] text-sm tracking-widest uppercase"
          >
            Premium 3D Printed Products
          </motion.p>
          
          <motion.h1
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.3, duration: 0.6 }}
            className="text-5xl md:text-7xl font-bold text-white leading-tight"
          >
            Portable Keychain
            <br />
            <span className="text-[var(--color-accent)]">Phone Stand</span>
          </motion.h1>
          
          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.4, duration: 0.6 }}
            className="text-xl text-[var(--muted-foreground)] max-w-2xl mx-auto"
          >
            Foldable • Compact • Customizable
          </motion.p>
          
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.5, duration: 0.6 }}
          >
            <Link href="/shop">
              <Button 
                size="lg" 
                className="bg-[var(--color-cta)] hover:bg-[var(--color-cta)]/90 text-white text-lg px-8 py-6 rounded-full font-semibold"
              >
                Shop Now
              </Button>
            </Link>
          </motion.div>
        </motion.div>
      </div>
      
      {/* Scroll indicator */}
      <motion.div
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 1, duration: 0.6 }}
        className="absolute bottom-8 left-1/2 -translate-x-1/2"
      >
        <div className="w-6 h-10 rounded-full border-2 border-white/30 flex items-start justify-center p-2">
          <motion.div
            animate={{ y: [0, 12, 0] }}
            transition={{ repeat: Infinity, duration: 1.5, ease: "easeInOut" }}
            className="w-1.5 h-1.5 bg-white rounded-full"
          />
        </div>
      </motion.div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/hero.tsx && grep -c "motion" components/landing/hero.tsx</automated>
  </verify>
  <done>Hero section with background, headline, subheadline, CTA button "Shop Now", scroll indicator, framer-motion animations</done>
</task>

<task type="auto">
  <name>Task 3: Build Featured Products showcase</name>
  <files>components/landing/featured-products.tsx, components/landing/product-card.tsx</files>
  <action>
Create the featured products section with 3 variant cards:

**Product Card component (`components/landing/product-card.tsx`):**
```tsx
"use client";

import { motion } from "framer-motion";
import Image from "next/image";
import Link from "next/link";
import { Card, CardContent } from "@/components/ui/card";

interface ProductCardProps {
  title: string;
  variant: string;
  price: number;
  imageUrl: string;
  href: string;
  delay?: number;
}

export function ProductCard({ title, variant, price, imageUrl, href, delay = 0 }: ProductCardProps) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ delay, duration: 0.5 }}
    >
      <Link href={href}>
        <Card className="bg-[var(--card-bg)] border-[var(--border)] hover:border-[var(--color-accent)] transition-colors overflow-hidden group">
          <CardContent className="p-0">
            <div className="relative aspect-square bg-[var(--muted)] overflow-hidden">
              <Image
                src={imageUrl}
                alt={variant}
                fill
                className="object-cover group-hover:scale-105 transition-transform duration-300"
              />
            </div>
            <div className="p-4 space-y-2">
              <p className="text-xs text-[var(--muted-foreground)] uppercase tracking-wider">{title}</p>
              <h3 className="text-lg font-semibold text-[var(--card-foreground)]">{variant}</h3>
              <p className="text-[var(--color-accent)] font-bold">₹{price}</p>
            </div>
          </CardContent>
        </Card>
      </Link>
    </motion.div>
  );
}
```

**Featured Products section (`components/landing/featured-products.tsx`):**
```tsx
"use client";

import { motion } from "framer-motion";
import { ProductCard } from "./product-card";

const products = [
  {
    id: "var_black",
    title: "Portable Keychain Phone Stand",
    variant: "Wakanda Black",
    price: 449,
    imageUrl: "/images/keychain-stand-front.jpg",
    handle: "portable-keychain-phone-stand",
  },
  {
    id: "var_grey",
    title: "Portable Keychain Phone Stand",
    variant: "Batman Grey",
    price: 449,
    imageUrl: "/images/keychain-stand-side.jpg",
    handle: "portable-keychain-phone-stand",
  },
  {
    id: "var_teal",
    title: "Portable Keychain Phone Stand",
    variant: "Captain Teal",
    price: 449,
    imageUrl: "/images/keychain-stand-lifestyle.jpg",
    handle: "portable-keychain-phone-stand",
  },
];

export function FeaturedProducts() {
  return (
    <section className="py-24 bg-[var(--background)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl md:text-4xl font-bold text-[var(--foreground)] mb-4">
            Choose Your Style
          </h2>
          <p className="text-[var(--muted-foreground)] max-w-2xl mx-auto">
            Three premium variants, each crafted with precision 3D printing
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {products.map((product, index) => (
            <ProductCard
              key={product.id}
              title={product.title}
              variant={product.variant}
              price={product.price}
              imageUrl={product.imageUrl}
              href={`/product/${product.handle}`}
              delay={index * 0.1}
            />
          ))}
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/featured-products.tsx components/landing/product-card.tsx</automated>
  </verify>
  <done>Featured products section with 3 variant cards, each showing product image, variant name, and price ₹449</done>
</task>

<task type="auto">
  <name>Task 4: Build Benefits section</name>
  <files>components/landing/benefits.tsx</files>
  <action>
Create the benefits section with 4 benefit cards:

**`components/landing/benefits.tsx`:**
```tsx
"use client";

import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";

const benefits = [
  {
    icon: "🏆",
    title: "Premium 3D Printed",
    description: "Crafted with precision using high-quality PLA/PETG materials for durability and style.",
  },
  {
    icon: "🔑",
    title: "Compact & Portable",
    description: "Designed to fit on your keychain. Take it anywhere - desk, travel, or on-the-go.",
  },
  {
    icon: "✂️",
    title: "Custom Engraving",
    description: "Personalize with your name or message (up to 12 characters) for a unique touch.",
  },
  {
    icon: "🎁",
    title: "Perfect Gift",
    description: "An innovative gift for tech enthusiasts, professionals, or anyone who loves gadgets.",
  },
];

export function Benefits() {
  return (
    <section className="py-24 bg-[var(--card-bg)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl md:text-4xl font-bold text-[var(--foreground)] mb-4">
            Why Choose Crucible Creations
          </h2>
          <p className="text-[var(--muted-foreground)] max-w-2xl mx-auto">
            Every product is designed with attention to detail and manufactured with care
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {benefits.map((benefit, index) => (
            <motion.div
              key={benefit.title}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1, duration: 0.5 }}
            >
              <Card className="bg-[var(--background)] border-[var(--border)] h-full">
                <CardContent className="p-6 text-center">
                  <div className="text-4xl mb-4">{benefit.icon}</div>
                  <h3 className="text-lg font-semibold text-[var(--foreground)] mb-2">
                    {benefit.title}
                  </h3>
                  <p className="text-sm text-[var(--muted-foreground)]">
                    {benefit.description}
                  </p>
                </CardContent>
              </Card>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/benefits.tsx && grep -c "motion" components/landing/benefits.tsx</automated>
  </verify>
  <done>Benefits section with 4 cards: Premium 3D Printed, Compact & Portable, Custom Engraving, Perfect Gift</done>
</task>

<task type="auto">
  <name>Task 5: Assemble landing page with Navbar, Hero, Featured Products, Benefits</name>
  <files>app/page.tsx</files>
  <action>
Update `app/page.tsx` to assemble all the landing page sections:

```tsx
import { Hero } from "@/components/landing/hero";
import { FeaturedProducts } from "@/components/landing/featured-products";
import { Benefits } from "@/components/landing/benefits";
import { Navbar } from "@/components/layout/navbar";
import { Footer } from "@/components/layout/footer";

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col bg-[var(--background)]">
      <Navbar />
      <main className="flex-1">
        <Hero />
        <FeaturedProducts />
        <Benefits />
      </main>
      <Footer />
    </div>
  );
}
```

This assembles:
- Navbar at top (fixed)
- Hero section (full viewport)
- Featured Products section
- Benefits section
- Footer at bottom
  </action>
  <verify>
    <automated>grep -c "Navbar\|Hero\|FeaturedProducts\|Benefits\|Footer" app/page.tsx</automated>
  </verify>
  <done>Landing page at app/page.tsx renders Navbar, Hero, Featured Products, Benefits, Footer</done>
</task>

</tasks>

<verification>
- Navbar has logo, navigation links, Shop Now CTA button
- Hero section has background image, headline, subheadline, CTA button, scroll indicator
- Featured Products shows 3 variant cards with images, names, prices
- Benefits shows 4 cards with icons and descriptions
- All sections use brand colors from CSS variables
- Dark mode styling applied by default
</verification>

<success_criteria>
- User sees hero with "Portable Keychain Phone Stand" headline and "Shop Now" CTA
- User sees 3 product variants: Wakanda Black, Batman Grey, Captain Teal with ₹449 each
- User sees 4 benefit cards explaining product value
- Page loads with dark theme (deep slate background)
- Framer Motion animations work on page load
- Navigation to /shop works when clicking CTA
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-landing-page/01-02-SUMMARY.md`
</output>
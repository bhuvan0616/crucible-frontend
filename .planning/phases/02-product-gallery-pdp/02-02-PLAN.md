---
phase: "02-product-gallery-pdp"
plan: "02"
type: "execute"
wave: 2
depends_on: ["02-01"]
files_modified:
  - "app/shop/page.tsx"
  - "components/shop/ProductGrid.tsx"
  - "components/shop/FilterBar.tsx"
  - "components/shop/ShopProductCard.tsx"
autonomous: true
requirements: ["GALL-01", "GALL-02", "GALL-04", "ARCH-04"]
must_haves:
  truths:
    - "User sees all products in a 280x280 grid"
    - "User can filter by Edition (Standard/Pro/Limited)"
    - "User can filter by Color swatches"
    - "User can search by title/variant/description (debounced 300ms)"
    - "User can sort (Featured/Price Low-High/High-Low/Newest)"
    - "Filter state persists in URL search params"
  artifacts:
    - path: "app/shop/page.tsx"
      provides: "Route /shop rendering full gallery"
    - path: "components/shop/ProductGrid.tsx"
      provides: "Grid container managing filter/search/sort state"
    - path: "components/shop/FilterBar.tsx"
      provides: "Edition pills, color swatches, search input, sort dropdown"
    - path: "components/shop/ShopProductCard.tsx"
      provides: "280x280 product card with Quick Add button on hover"
  key_links:
    - from: "components/shop/ProductGrid.tsx"
      to: "lib/data/products.ts"
      via: "import products, getProductById"
    - from: "components/shop/ProductGrid.tsx"
      to: "components/shop/FilterBar.tsx"
      via: "filter/sort state props"
    - from: "components/shop/ShopProductCard.tsx"
      to: "app/product/[id]/page.tsx"
      via: "next/link to /product/{id}"
---

<objective>
Build the product gallery page (`/shop`) with ProductGrid, FilterBar, and ShopProductCard components. Filter by Edition/Color, search with 300ms debounce, sort with dropdown. URL search params for shareable filter state.

Purpose: Enable product browsing with full filter/search/sort capability.
Output: `/shop` route with gallery grid, filter bar, and product cards
</objective>

<context>
@.planning/phases/02-product-gallery-pdp/02-01-PLAN.md
@.planning/phases/02-product-gallery-pdp/RESEARCH.md (Section 1, 4, 5)

From 02-01-PLAN.md:
- Types at `types/index.ts`, CartStore at `store/cartStore.ts`, Product data at `lib/data/products.ts`
- No UI components yet

From RESEARCH.md:
- Filter pills: Edition Standard | Pro | Limited
- Color swatches with count badges
- URL search params: `/shop?edition=pro&color=stealth-black`
- Sort options: Featured, Price: Low to High, Price: High to Low, Newest
- Search: debounced 300ms, clears with X button
- Product card: 280x280 image, compact info stack, Quick Add on hover, links to `/product/{id}`
- Empty state: "No products match your search"
</context>

<interfaces>

Key types from `types/index.ts` (Wave 1 output):

```typescript
interface Product {
  id: string;
  title: string;
  variant: string;
  description: string;
  price: number;  // paise
  images: string[];
  edition: 'standard' | 'pro' | 'limited';
  colors: ColorVariant[];
  specs: Record<string, string>;
  featured: boolean;
}

interface ColorVariant {
  name: string;
  hex: string;
  available: boolean;
}
```

Key exports from `lib/data/products.ts` (Wave 1 output):

```typescript
export const products: Product[];
export function getProductById(id: string): Product | undefined;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterBar component</name>
  <files>components/shop/FilterBar.tsx</files>
  <action>
    Create `components/shop/FilterBar.tsx` managing filter/sort/search state.

    Props: receives current filters (edition, color, search, sort) and onChange callbacks.

    Features:
    - Edition filter: 3 pill toggles (Standard / Pro / Limited) — horizontal scroll on mobile
    - Color filter: Color swatches with count badges (from product data)
    - Search input: debounced 300ms (use setTimeout + cleanup), X button to clear
    - Sort dropdown: "Featured" (default), "Price: Low to High", "Price: High to Low", "Newest"
    - Sticky below navbar

    Use `useRouter` / `useSearchParams` from Next.js to sync filter state to URL params (`?edition=pro&color=stealth-black`). URL params are source of truth — initialized from URL on mount, updated on change.

    Import Product type from `types/index.ts` to read available colors for swatches.
  </action>
  <verify>
    <automated>grep -c "edition\|color\|search\|sort" components/shop/FilterBar.tsx</automated>
  </verify>
  <done>FilterBar has Edition pills, Color swatches, Search with debounce, Sort dropdown, URL sync</done>
</task>

<task type="auto">
  <name>Task 2: Create ShopProductCard component</name>
  <files>components/shop/ShopProductCard.tsx</files>
  <action>
    Create `components/shop/ShopProductCard.tsx` — compact card variant for gallery grid.

    Props: `product: Product`

    Layout:
    - 280x280 aspect-square product image (aspect-square class)
    - Info stack below: title (muted text-sm), variant name (font-semibold), price in lime accent
    - Quick Add button appears on hover (position absolute, framer-motion fade)
    - Links entire card to `/product/{product.id}` via next/link

    Style:
    - Card background: var(--card-bg) or bg-slate-900/50
    - Border radius: 18px matching design system
    - Hover: subtle scale(1.02) transform

    Note: Quick Add button styling only (no cart wiring yet — that comes in 02-04).
  </action>
  <verify>
    <automated>grep -c "280\|aspect-square\|Quick Add" components/shop/ShopProductCard.tsx</automated>
  </verify>
  <done>ShopProductCard renders at 280x280 with image, variant, price, and hover Quick Add button</done>
</task>

<task type="auto">
  <name>Task 3: Create ProductGrid component</name>
  <files>components/shop/ProductGrid.tsx</files>
  <action>
    Create `components/shop/ProductGrid.tsx` — grid container orchestrating filter state.

    Import `products` from `lib/data/products.ts` as the data source.

    State: holds computed filtered+sorted product list derived from FilterBar filter changes.

    Logic:
    - Filter by edition: keep products where product.edition matches selected (or all if none)
    - Filter by color: keep products that have at least one ColorVariant.name matching selected (or all if none)
    - Search filter: lowercase include on title + variant + description (debounced input from FilterBar)
    - Sort: Featured (featured first), Price Low→High, Price High→Low, Newest (by id order)
    - Empty state: "No products match your search" centered message

    Renders:
    - FilterBar at top (sticky)
    - CSS grid: 2 columns mobile, 3 columns tablet, 4 columns desktop (responsive grid-cols-*)
    - Maps filtered products → ShopProductCard
  </action>
  <verify>
    <automated>grep -c "filter\|sort\|products\|ShopProductCard" components/shop/ProductGrid.tsx</automated>
  </verify>
  <done>ProductGrid applies filters/sort to products array and renders ShopProductCard grid</done>
</task>

<task type="auto">
  <name>Task 4: Create /shop route</name>
  <files>app/shop/page.tsx</files>
  <action>
    Create `app/shop/page.tsx` — route page for /shop.

    Imports and renders `<ProductGrid />`. No additional logic — ProductGrid owns all filter state.

    Page metadata: title "Shop — Crucible Creations"
    No additional layout changes — uses root layout.
  </action>
  <verify>
    <automated>ls app/shop/page.tsx && grep -c "ProductGrid" app/shop/page.tsx</automated>
  </verify>
  <done>/shop route exists and renders ProductGrid</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FilterBar input → ProductGrid | Untrusted user search text processed client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-GALL-01 | Tampering | URL filter params | mitigate | Params are validated against known enum values (edition, color) before applying |
| T-GALL-02 | Denial | Large product set search | accept | Client-side filter; Phase 2 has ~10 products, not a performance concern |
| T-GALL-03 | Information Disclosure | Search reveals product names | accept | Search is a client-side filter against public product data |
</threat_model>

<verification>
1. `ls app/shop/page.tsx` — route file exists
2. `grep -c "ProductGrid\|FilterBar\|ShopProductCard" components/shop/ProductGrid.tsx` — components integrated
3. `grep -c "edition\|color\|search\|sort" components/shop/FilterBar.tsx` — all filter features present
4. Visit `/shop` in browser — grid renders with products, filters work, search debounces
</verification>

<success_criteria>
- /shop renders product grid
- Edition filter pills filter the grid
- Color swatches filter the grid  
- Search debounces 300ms and filters by title/variant/description
- Sort dropdown changes grid order
- URL params update when filters change
- ShopProductCard links to /product/{id}
- All files committed
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-gallery-pdp/02-02-SUMMARY.md`
</output>
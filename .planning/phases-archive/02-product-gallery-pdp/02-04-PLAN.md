---
phase: "02-product-gallery-pdp"
plan: "04"
type: "execute"
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - "components/shop/ShopProductCard.tsx"
  - "components/product/AddToCartSection.tsx"
autonomous: true
requirements: ["GALL-03"]
must_haves:
  truths:
    - "Quick Add from ShopProductCard adds item to Zustand cart"
    - "Cart slide-over shows added items from ShopProductCard"
    - "Cart slide-over shows items from full PDP AddToCartSection"
    - "Cart slide-over accessible via navbar cart icon"
  artifacts:
    - path: "components/shop/ShopProductCard.tsx"
      provides: "Quick Add button wired to cartStore.addItem"
    - path: "components/layout/navbar.tsx"
      provides: "Cart icon triggering slide-over"
    - path: "components/cart/CartSlideOver.tsx"
      provides: "Slide-over panel showing cart items"
  key_links:
    - from: "components/shop/ShopProductCard.tsx"
      to: "store/cartStore.ts"
      via: "useCartStore.addItem"
    - from: "components/layout/navbar.tsx"
      to: "components/cart/CartSlideOver.tsx"
      via: "open/close state"
    - from: "components/cart/CartSlideOver.tsx"
      to: "store/cartStore.ts"
      via: "useCartStore items selector"
---

<objective>
Wire Quick Add functionality from gallery ShopProductCard → Zustand cart, and create the cart slide-over panel triggered from navbar. Completes the cart integration started in Wave 2 PDP components.

Purpose: Cart store connection for quick-add in gallery, cart visibility via slide-over.
Output: Quick-add wired ShopProductCard, CartSlideOver component, navbar cart icon wired
</objective>

<context>
@.planning/phases/02-product-gallery-pdp/02-02-PLAN.md
@.planning/phases/02-product-gallery-pdp/02-03-PLAN.md
@.planning/phases/02-product-gallery-pdp/RESEARCH.md (Section 5)

From 02-02-PLAN.md:
- ShopProductCard renders Quick Add button (styled, not wired)
- ProductGrid renders the grid

From 02-03-PLAN.md:
- AddToCartSection calls cartStore.addItem on button click
- cartStore.addItem signature: `addItem({ productId, variant, customization, quantity, price, imageUrl })`

From RESEARCH.md:
- Cart slide-over: not a separate page, slide-over from right side
- Triggered by cart icon in navbar
- Shows items, quantities, subtotal
- "Checkout" button navigates to /checkout (Phase 3)
</context>

<interfaces>

Key store from `store/cartStore.ts` (Wave 1 output):

```typescript
interface CartStore {
  items: CartItem[];
  addItem: (item: Omit<CartItem, 'id'>) => void;
  removeItem: (id: string) => void;
  updateQuantity: (id: string, quantity: number) => void;
  clearCart: () => void;
}

// Selectors
const selectCartTotal = (state: CartStore) => 
  state.items.reduce((sum, item) => sum + item.price * item.quantity, 0);
const selectCartItemCount = (state: CartStore) => 
  state.items.reduce((sum, item) => sum + item.quantity, 0);
```

CartItem type from `types/index.ts`:

```typescript
interface CartItem {
  id: string;
  productId: string;
  variant: string;
  customization: string;
  quantity: number;
  price: number;
  imageUrl: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire Quick Add on ShopProductCard</name>
  <files>components/shop/ShopProductCard.tsx</files>
  <action>
    Update `components/shop/ShopProductCard.tsx` to wire the Quick Add button to Zustand cart.

    Quick Add behavior (simplified — no customization in gallery quick-add):
    - Uses default variant (first color from product.colors)
    - quantity: 1
    - customization: "" (empty string for quick-add)
    - price: product.price
    - imageUrl: product.images[0]

    On click:
    - Call `useCartStore.getState().addItem({ productId: product.id, variant: firstColor, customization: '', quantity: 1, price: product.price, imageUrl: product.images[0] })`
    - Show brief "Added!" feedback on the button (same pattern as AddToCartSection: 1.5s)

    Note: Import useCartStore from store/cartStore.ts. Use `.getState()` to call outside of React hook context.
  </action>
  <verify>
    <automated>grep -c "addItem\|useCartStore" components/shop/ShopProductCard.tsx</automated>
  </verify>
  <done>Quick Add button on ShopProductCard adds item to cart store with correct data</done>
</task>

<task type="auto">
  <name>Task 2: Create CartSlideOver component</name>
  <files>components/cart/CartSlideOver.tsx</files>
  <action>
    Create `components/cart/CartSlideOver.tsx` — slide-over panel displaying cart contents.

    Import useCartStore, selectCartTotal, selectCartItemCount from store/cartStore.ts.

    Layout:
    - Slide-over from right edge (off-canvas, slides in)
    - Header: "Your Cart (N items)" + close button
    - Item list: each item shows image, title, variant, customization text (read-only), quantity controls, price
    - Subtotal section at bottom
    - "Checkout" button (navigates to /checkout — Phase 3 route, disabled/placeholder for now)
    - Backdrop overlay (click to close)

    State: `isOpen: boolean` prop to control visibility.

    Use framer-motion for slide animation (AnimatePresence + motion.div).

    If cart is empty: show "Your cart is empty" message with "Continue Shopping" link to /shop.
  </action>
  <verify>
    <automated>grep -c "CartSlideOver\|useCartStore\|items" components/cart/CartSlideOver.tsx</automated>
  </verify>
  <done>CartSlideOver renders cart items with quantity controls, subtotal, and checkout button</done>
</task>

<task type="auto">
  <name>Task 3: Wire cart icon in navbar to CartSlideOver</name>
  <files>components/layout/navbar.tsx</files>
  <action>
    Update `components/layout/navbar.tsx` to wire cart icon to CartSlideOver.

    Add:
    - Cart icon (shopping bag icon from lucide-react or inline SVG)
    - Badge showing cart item count (from useCartStore with selectCartItemCount selector)
    - onClick: sets isOpen(true) for CartSlideOver

    Import CartSlideOver component.
    Import useCartStore and selectCartItemCount from store/cartStore.ts.

    CartSlideOver should be rendered in the navbar (or as a portal in layout) but controlled by navbar state.
  </action>
  <verify>
    <automated>grep -c "CartSlideOver\|useCartStore\|cart" components/layout/navbar.tsx</automated>
  </verify>
  <done>Navbar cart icon opens CartSlideOver and shows item count badge</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShopProductCard → cartStore | Quick-add bypasses customization review; empty customization allowed |
| CartSlideOver → cartStore | Reads items from persisted localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CART-01 | Tampering | Quick-add with empty customization | accept | Empty string is intentional for quick-add; Phase 3 checkout validates |
| T-CART-02 | Denial | Rapid cart additions | accept | Zustand is synchronous; no server-side rate limit needed at Phase 2 scale |
| T-CART-03 | Information Disclosure | Cart contents in localStorage | mitigate | localStorage is browser-local; no cross-origin data exposure |
</threat_model>

<verification>
1. `grep -c "addItem\|useCartStore" components/shop/ShopProductCard.tsx` — quick-add wired
2. `ls components/cart/CartSlideOver.tsx` — slide-over component exists
3. `grep -c "CartSlideOver" components/layout/navbar.tsx` — navbar wired to slide-over
4. Visit /shop, click Quick Add → cart badge count updates
5. Visit /product/{id}, add to cart → cart badge count updates
6. Click cart icon → slide-over shows items with correct details
</verification>

<success_criteria>
- Quick Add from ShopProductCard adds correct item to cart (default variant, qty 1, empty customization)
- Cart badge in navbar shows accurate item count
- Clicking cart icon opens CartSlideOver
- CartSlideOver shows all items with variant, customization (read-only), quantity, price
- Empty cart shows "Your cart is empty" message
- All files committed
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-gallery-pdp/02-04-SUMMARY.md`
</output>
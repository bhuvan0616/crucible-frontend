---
phase: "02-product-gallery-pdp"
plan: "03"
type: "execute"
wave: 2
depends_on: ["02-01"]
files_modified:
  - "app/product/[id]/page.tsx"
  - "components/product/ProductGallery.tsx"
  - "components/product/ProductInfo.tsx"
  - "components/product/VariantSelector.tsx"
  - "components/product/CustomizationInput.tsx"
  - "components/product/AddToCartSection.tsx"
autonomous: true
requirements: ["PDP-01", "PDP-02", "PDP-03", "PDP-04", "PDP-05", "PDP-06", "PDP-07", "PDP-08", "PDP-09", "PDP-10", "PDP-11"]
must_haves:
  truths:
    - "PDP renders two-column layout (image gallery 60% left, sticky info 40% right)"
    - "User can browse product images via thumbnail strip"
    - "User can select color variant via swatches"
    - "User can enter custom text (max 12 chars) with live counter"
    - "User can adjust quantity and add to cart"
    - "Product description, specs, and shipping info are displayed"
  artifacts:
    - path: "app/product/[id]/page.tsx"
      provides: "Route /product/[id] with dynamic product loading"
    - path: "components/product/ProductGallery.tsx"
      provides: "Large image display with thumbnail navigation"
    - path: "components/product/ProductInfo.tsx"
      provides: "Sticky right column with title, price, variant, customization, add to cart"
    - path: "components/product/VariantSelector.tsx"
      provides: "Color swatch selector with checkmark and tooltip"
    - path: "components/product/CustomizationInput.tsx"
      provides: "Text input with 12-char limit and live counter"
    - path: "components/product/AddToCartSection.tsx"
      provides: "Quantity selector and Add to Cart button"
  key_links:
    - from: "app/product/[id]/page.tsx"
      to: "lib/data/products.ts"
      via: "getProductById(params.id)"
    - from: "components/product/ProductInfo.tsx"
      to: "components/product/VariantSelector.tsx"
      via: "selectedVariant state"
    - from: "components/product/ProductInfo.tsx"
      to: "components/product/AddToCartSection.tsx"
      via: "variant + customization props"
---

<objective>
Build the complete Product Detail Page (`/product/[id]`) with two-column layout: image gallery left, sticky product info right. Variant selector with color swatches, 12-char customization input with live counter, quantity selector, and Add to Cart.

Purpose: Enable detailed product viewing, variant selection, customization, and cart addition.
Output: `/product/[id]` route with 5 PDP component files
</objective>

<context>
@.planning/phases/02-product-gallery-pdp/02-01-PLAN.md
@.planning/phases/02-product-gallery-pdp/RESEARCH.md (Section 1, 4)

From 02-01-PLAN.md:
- Types at `types/index.ts`, CartStore at `store/cartStore.ts`, Product data at `lib/data/products.ts`
- No UI components yet

From RESEARCH.md:
- PDP: Two-column — image gallery (60%) left, sticky product info (40%) right
- Image gallery: main image + thumbnail strip below (click thumbnail to change main)
- Variant selector: 32x32 color swatches, white checkmark on selected, strikethrough + reduced opacity for unavailable, tooltip on hover
- Customization input: 12-char max, live counter `5/12` format, disabled at limit
- ProductInfo includes: title, price (lime accent), variant name, description, specs (table), shipping info

Component file responsibilities:
- ProductGallery.tsx: image viewer with thumbnails
- ProductInfo.tsx: sticky column assembling title/price/variant/customization/add-to-cart
- VariantSelector.tsx: color swatches with selection state
- CustomizationInput.tsx: text input with 12-char limit
- AddToCartSection.tsx: quantity + Add to Cart button
</context>

<interfaces>

Key types from `types/index.ts` (Wave 1 output):

```typescript
interface Product {
  id: string;
  title: string;
  variant: string;
  description: string;
  price: number;  // paise
  images: string[];
  edition: 'standard' | 'pro' | 'limited';
  colors: ColorVariant[];
  specs: Record<string, string>;
  featured: boolean;
}

interface ColorVariant {
  name: string;
  hex: string;
  available: boolean;
}
```

Key store from `store/cartStore.ts` (Wave 1 output):

```typescript
interface CartStore {
  items: CartItem[];
  addItem: (item: Omit<CartItem, 'id'>) => void;
  removeItem: (id: string) => void;
  updateQuantity: (id: string, quantity: number) => void;
  clearCart: () => void;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductGallery component</name>
  <files>components/product/ProductGallery.tsx</files>
  <action>
    Create `components/product/ProductGallery.tsx` — image gallery for PDP.

    Props: `images: string[]`, `productTitle: string`

    Layout:
    - Main image: large display (fill container or fixed aspect)
    - Thumbnail strip: horizontal row below main image
    - Click thumbnail → updates main image (active thumbnail highlighted)
    - Thumbnail size: 64x64

    State: `selectedImageIndex` (default 0).

    If only 1 image, hide thumbnail strip.
    Use Next.js Image component for optimization.
  </action>
  <verify>
    <automated>grep -c "selectedImage\|thumbnail" components/product/ProductGallery.tsx</automated>
  </verify>
  <done>ProductGallery renders main image and thumbnail strip with click-to-select</done>
</task>

<task type="auto">
  <name>Task 2: Create VariantSelector component</name>
  <files>components/product/VariantSelector.tsx</files>
  <action>
    Create `components/product/VariantSelector.tsx` — color swatch selector.

    Props: `colors: ColorVariant[]`, `selected: string`, `onChange: (colorName: string) => void`

    Layout:
    - 32x32px circular swatches with background-color = hex value
    - Selected: white checkmark overlay (absolute positioned)
    - Unavailable: diagonal strikethrough (CSS ::before pseudo-element), opacity 0.4
    - Tooltip on hover: color name (using title attribute or data-tip)

    Import ColorVariant from `types/index.ts`.
  </action>
  <verify>
    <automated>grep -c "32\|ColorVariant\|checkmark" components/product/VariantSelector.tsx</automated>
  </verify>
  <done>VariantSelector renders color swatches with selection state and unavailable styling</done>
</task>

<task type="auto">
  <name>Task 3: Create CustomizationInput component</name>
  <files>components/product/CustomizationInput.tsx</files>
  <action>
    Create `components/product/CustomizationInput.tsx` — text input with 12-char limit.

    Props: `value: string`, `onChange: (text: string) => void`

    Features:
    - Text input with `maxLength={12}` enforced
    - Live character counter displayed as `N/12` (current/max)
    - Counter color: muted below 8, warning (orange) at 10-11, disabled state at 12
    - Placeholder: "Your text here..."
    - When at 12 chars, input should visually indicate disabled state (opacity or no-cursor)
    - Enforces 12-char limit client-side only (Phase 2 mock)

    Import nothing from cart store — pure presentational component.
  </action>
  <verify>
    <automated>grep -c "12\|maxLength\|counter" components/product/CustomizationInput.tsx</automated>
  </verify>
  <done>CustomizationInput limits to 12 chars with live counter display</done>
</task>

<task type="auto">
  <name>Task 4: Create AddToCartSection component</name>
  <files>components/product/AddToCartSection.tsx</files>
  <action>
    Create `components/product/AddToCartSection.tsx` — quantity selector + Add to Cart button.

    Props: `product: Product`, `selectedVariant: string`, `customization: string`

    Layout:
    - Quantity selector: minus/plus buttons with number display between (min: 1, max: 99)
    - "Add to Cart" button: full width, orange CTA color (var(--color-cta))
    - Button disabled if no variant selected or cart is processing

    On click:
    - Call `useCartStore.getState().addItem({ productId, variant, customization, quantity, price, imageUrl })`
    - Show brief success feedback (button text changes to "Added!" for 1.5s, then resets)

    Import useCartStore from `store/cartStore.ts` and CartItem from `types/index.ts`.
  </action>
  <verify>
    <automated>grep -c "addItem\|quantity\|Add to Cart" components/product/AddToCartSection.tsx</automated>
  </verify>
  <done>AddToCartSection renders quantity controls and Add to Cart button that calls cartStore.addItem</done>
</task>

<task type="auto">
  <name>Task 5: Create ProductInfo and /product/[id] route</name>
  <files>components/product/ProductInfo.tsx, app/product/[id]/page.tsx</files>
  <action>
    Create `components/product/ProductInfo.tsx` — sticky right column orchestrator.

    Props: `product: Product`

    This component assembles the sticky info panel:
    - Title (text-xl font-bold), price in lime accent, edition badge
    - VariantSelector (with selectedVariant state)
    - CustomizationInput (with customizationText state)
    - AddToCartSection (passes selectedVariant + customizationText)
    - Description section
    - Specs table (Object.entries from specs)
    - Shipping info section

    State managed here: selectedVariant (from product.colors[0].name), customizationText.

    --- 

    Create `app/product/[id]/page.tsx` — PDP route.

    Logic:
    - `const { id } = useParams()` to get product ID
    - `const product = getProductById(id)` from lib/data/products.ts
    - If product not found: render "Product not found" message (or notFound() from next/navigation)
    - If found: render two-column layout with ProductGallery (left, 60%) and ProductInfo (right, 40%, sticky)

    Two-column layout: CSS flex or grid, left column scrolls independently, right column sticky via `position: sticky; top: 80px`.

    Page metadata: title "{product.title} — Crucible Creations"
  </action>
  <verify>
    <automated>grep -c "ProductGallery\|ProductInfo\|position: sticky\|useParams\|getProductById" app/product/[id]/page.tsx</automated>
  </verify>
  <done>ProductInfo assembles all PDP sections; /product/[id] renders two-column layout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL /product/[id] → ProductInfo | Untrusted id param; getProductById returns undefined → notFound() |
| CustomizationInput → CartItem | User text stored directly; 12-char client-side limit only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PDP-01 | Tampering | URL id param | mitigate | getProductById returns undefined for unknown IDs; Next.js notFound() handles gracefully |
| T-PDP-02 | Information Disclosure | Customization text stored in cart | accept | No PII; custom text is public-facing by design |
| T-PDP-03 | Denial | Rapid add-to-cart clicks | mitigate | Button shows "Added!" for 1.5s; useCartStore.addItem is idempotent-friendly |
</threat_model>

<verification>
1. `ls app/product/[id]/page.tsx` — route exists
2. `grep -c "position: sticky" components/product/ProductInfo.tsx` — sticky panel configured
3. `grep -c "maxLength.*12\|12.*maxLength" components/product/CustomizationInput.tsx` — char limit enforced
4. `grep -c "addItem\|useCartStore" components/product/AddToCartSection.tsx` — cart store connected
5. Visit `/product/{id}` — two-column layout, variant selector, customization input, add to cart all work
</verification>

<success_criteria>
- /product/[id] renders two-column layout (image gallery left, sticky info right)
- ProductGallery shows main image and clickable thumbnails
- VariantSelector shows color swatches with checkmark on selected
- CustomizationInput shows live counter (N/12) and enforces 12-char limit
- AddToCartSection quantity selector works and button adds to Zustand cart
- Product description, specs table, and shipping info sections visible
- All files committed
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-gallery-pdp/02-03-SUMMARY.md`
</output>
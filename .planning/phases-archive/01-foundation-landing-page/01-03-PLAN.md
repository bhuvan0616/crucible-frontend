---
phase: 01-foundation-landing-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/landing/how-it-works.tsx
  - components/landing/usage-scenarios.tsx
  - components/landing/testimonials.tsx
  - components/landing/newsletter.tsx
autonomous: true
requirements:
  - LAND-04
  - LAND-05
  - LAND-06
  - LAND-07
must_haves:
  truths:
    - "User can see how the product works in 3 steps"
    - "User can see 4 usage scenario cards"
    - "User can see testimonials from mock customers"
    - "User can see newsletter signup form"
  artifacts:
    - path: "components/landing/how-it-works.tsx"
      provides: "3-step usage instructions with icons"
    - path: "components/landing/usage-scenarios.tsx"
      provides: "4 usage scenario cards with icons and descriptions"
    - path: "components/landing/testimonials.tsx"
      provides: "3 mock testimonials with avatar, name, text"
    - path: "components/landing/newsletter.tsx"
      provides: "Email input + submit button (UI only)"
    - path: "app/page.tsx"
      provides: "Updated landing page assembling all sections"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/how-it-works.tsx"
      via: "import HowItWorks from components/landing/how-it-works"
---

<objective>
Build the remaining landing page sections: How it Works, Usage Scenarios, Testimonials, and Newsletter signup.
</objective>

<execution_context>
@/home/bhuvanesh/Workspace/NextProjects/crucible/app/page.tsx
@/home/bhuvanesh/Workspace/NextProjects/crucible/crucible-creations-prd.md (Section 4.1: Landing Page)
@/home/bhuvanesh/Workspace/NextProjects/crucible/components/landing/benefits.tsx (reuse pattern)
</execution_context>

<context>
## Requirements for This Plan

**LAND-04: How it works section with usage steps**
- 3-step process: Unfold, Adjust, Enjoy
- Icon + step number + title + description
- Visual connector between steps

**LAND-05: Usage scenarios section**
- 4 scenarios: Desk Work, Travel, Video Calls, Gaming
- Icon + scenario name + brief description
- Card layout matching Benefits section style

**LAND-06: Testimonials section (mock data)**
- 3 testimonials with:
  - Avatar image (placeholder)
  - Name and role
  - Star rating (5 stars)
  - Quote text
- Mock data: realistic names and quotes

**LAND-07: Newsletter signup form**
- Email input field
- Submit button "Subscribe"
- Helper text: "Get updates on new products"
- UI only - no backend (Phase 3 note: could wire to Mailchimp/etc)

## Design Pattern

All sections follow the same pattern as Benefits from Plan 02:
- Section title and subtitle
- Grid of cards (2x2 for scenarios, 3 columns for testimonials)
- Motion animations for scroll reveal
- Brand colors from CSS variables

## Component File Structure

```
components/landing/
├── how-it-works.tsx     # 3-step horizontal process
├── usage-scenarios.tsx   # 4 scenario cards
├── testimonials.tsx     # 3 testimonial cards
└── newsletter.tsx       # Email signup form
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build How It Works section (3 steps)</name>
  <files>components/landing/how-it-works.tsx</files>
  <action>
Create the "How It Works" section with 3 horizontal steps:

**`components/landing/how-it-works.tsx`:**
```tsx
"use client";

import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";

const steps = [
  {
    number: "01",
    title: "Unfold",
    description: "Pull apart the folded stand from your keychain. The compact design unfolds in seconds.",
    icon: "🔓",
  },
  {
    number: "02",
    title: "Adjust",
    description: "Rotate the adjustable arms to set your preferred viewing angle. Holds securely at any position.",
    icon: "🔧",
  },
  {
    number: "03",
    title: "Enjoy",
    description: "Place your phone and enjoy hands-free viewing. Perfect for video calls, streaming, or cooking.",
    icon: "📱",
  },
];

export function HowItWorks() {
  return (
    <section className="py-24 bg-[var(--background)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl md:text-4xl font-bold text-[var(--foreground)] mb-4">
            How It Works
          </h2>
          <p className="text-[var(--muted-foreground)] max-w-2xl mx-auto">
            Simple, intuitive, and ready in seconds
          </p>
        </motion.div>

        <div className="relative">
          {/* Connection line */}
          <div className="hidden md:block absolute top-24 left-1/4 right-1/4 h-0.5 bg-gradient-to-r from-transparent via-[var(--color-accent)] to-transparent" />
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {steps.map((step, index) => (
              <motion.div
                key={step.number}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: index * 0.15, duration: 0.5 }}
                className="relative"
              >
                <Card className="bg-[var(--card-bg)] border-[var(--border)] h-full">
                  <CardContent className="p-8 text-center">
                    <div className="relative inline-flex mb-6">
                      <div className="w-16 h-16 rounded-full bg-[var(--color-accent)]/20 flex items-center justify-center">
                        <span className="text-3xl">{step.icon}</span>
                      </div>
                      <span className="absolute -top-2 -right-2 w-8 h-8 rounded-full bg-[var(--color-accent)] text-white font-bold flex items-center justify-center text-sm">
                        {step.number}
                      </span>
                    </div>
                    <h3 className="text-xl font-bold text-[var(--foreground)] mb-3">
                      {step.title}
                    </h3>
                    <p className="text-[var(--muted-foreground)]">
                      {step.description}
                    </p>
                  </CardContent>
                </Card>
              </motion.div>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/how-it-works.tsx && grep -c "Unfold\|Adjust\|Enjoy" components/landing/how-it-works.tsx</automated>
  </verify>
  <done>How It Works section with 3 steps: Unfold (🔓), Adjust (🔧), Enjoy (📱)</done>
</task>

<task type="auto">
  <name>Task 2: Build Usage Scenarios section</name>
  <files>components/landing/usage-scenarios.tsx</files>
  <action>
Create the Usage Scenarios section with 4 scenario cards:

**`components/landing/usage-scenarios.tsx`:**
```tsx
"use client";

import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";

const scenarios = [
  {
    icon: "💼",
    title: "Desk Work",
    description: "Keep your phone at eye level during long work sessions. Perfect for video calls and multitasking.",
  },
  {
    icon: "✈️",
    title: "Travel",
    description: "Compact enough to take anywhere. Use on plane trays, hotel desks, or any flat surface.",
  },
  {
    icon: "🎥",
    title: "Video Calls",
    description: "Hands-free video calls with friends, family, or colleagues. Set it and forget it.",
  },
  {
    icon: "🎮",
    title: "Gaming",
    description: "Prop up your phone for mobile gaming sessions. Comfortable viewing without holding the device.",
  },
];

export function UsageScenarios() {
  return (
    <section className="py-24 bg-[var(--card-bg)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl md:text-4xl font-bold text-[var(--foreground)] mb-4">
            Perfect For Every Situation
          </h2>
          <p className="text-[var(--muted-foreground)] max-w-2xl mx-auto">
            One product, endless possibilities
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {scenarios.map((scenario, index) => (
            <motion.div
              key={scenario.title}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1, duration: 0.5 }}
            >
              <Card className="bg-[var(--background)] border-[var(--border)] h-full hover:border-[var(--color-accent)] transition-colors">
                <CardContent className="p-6 text-center">
                  <div className="text-4xl mb-4">{scenario.icon}</div>
                  <h3 className="text-lg font-semibold text-[var(--foreground)] mb-2">
                    {scenario.title}
                  </h3>
                  <p className="text-sm text-[var(--muted-foreground)]">
                    {scenario.description}
                  </p>
                </CardContent>
              </Card>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/usage-scenarios.tsx && grep -c "Desk Work\|Travel\|Video Calls\|Gaming" components/landing/usage-scenarios.tsx</automated>
  </verify>
  <done>Usage Scenarios section with 4 cards: Desk Work, Travel, Video Calls, Gaming</done>
</task>

<task type="auto">
  <name>Task 3: Build Testimonials section</name>
  <files>components/landing/testimonials.tsx</files>
  <action>
Create the Testimonials section with 3 mock testimonials:

**`components/landing/testimonials.tsx`:**
```tsx
"use client";

import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";

const testimonials = [
  {
    name: "Priya Sharma",
    role: "Software Engineer, Bangalore",
    avatar: "👩‍💻",
    rating: 5,
    quote: "This phone stand is genius! I use it daily for video calls and it's so convenient to have on my keychain. The 3D printed quality is impressive.",
  },
  {
    name: "Rahul Mehta",
    role: "Digital Nomad, Goa",
    avatar: "🧑‍💻",
    rating: 5,
    quote: "Perfect for my travel setup. It's so compact and the custom engraving makes it uniquely mine. Worth every rupee!",
  },
  {
    name: "Ananya Krishnan",
    role: "Content Creator, Mumbai",
    avatar: "👩‍🎨",
    rating: 5,
    quote: "The best impulse buy ever. My audience always asks about my phone stand and I love showing it off! The teal color is stunning.",
  },
];

function StarRating({ count }: { count: number }) {
  return (
    <div className="flex items-center gap-1 mb-3">
      {[...Array(count)].map((_, i) => (
        <span key={i} className="text-[var(--color-cta)]">★</span>
      ))}
    </div>
  );
}

export function Testimonials() {
  return (
    <section className="py-24 bg-[var(--background)]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl md:text-4xl font-bold text-[var(--foreground)] mb-4">
            What Our Customers Say
          </h2>
          <p className="text-[var(--muted-foreground)] max-w-2xl mx-auto">
            Real reviews from real people who love their stands
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {testimonials.map((testimonial, index) => (
            <motion.div
              key={testimonial.name}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1, duration: 0.5 }}
            >
              <Card className="bg-[var(--card-bg)] border-[var(--border)] h-full">
                <CardContent className="p-6">
                  <div className="flex items-center gap-4 mb-4">
                    <div className="w-12 h-12 rounded-full bg-[var(--muted)] flex items-center justify-center text-2xl">
                      {testimonial.avatar}
                    </div>
                    <div>
                      <h4 className="font-semibold text-[var(--foreground)]">{testimonial.name}</h4>
                      <p className="text-xs text-[var(--muted-foreground)]">{testimonial.role}</p>
                    </div>
                  </div>
                  <StarRating count={testimonial.rating} />
                  <p className="text-[var(--muted-foreground)] italic">"{testimonial.quote}"</p>
                </CardContent>
              </Card>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/testimonials.tsx && grep -c "Priya\|Rahul\|Ananya" components/landing/testimonials.tsx</automated>
  </verify>
  <done>Testimonials section with 3 mock testimonials: Priya Sharma, Rahul Mehta, Ananya Krishnan</done>
</task>

<task type="auto">
  <name>Task 4: Build Newsletter signup form</name>
  <files>components/landing/newsletter.tsx</files>
  <action>
Create the Newsletter signup section with email form:

**`components/landing/newsletter.tsx`:**
```tsx
"use client";

import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Card, CardContent } from "@/components/ui/card";

export function Newsletter() {
  return (
    <section className="py-24 bg-[var(--card-bg)]">
      <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.5 }}
        >
          <Card className="bg-[var(--background)] border-[var(--border)]">
            <CardContent className="p-8 md:p-12 text-center">
              <div className="mb-6">
                <span className="text-4xl">📬</span>
              </div>
              <h2 className="text-2xl md:text-3xl font-bold text-[var(--foreground)] mb-4">
                Stay in the Loop
              </h2>
              <p className="text-[var(--muted-foreground)] mb-8 max-w-lg mx-auto">
                Get updates on new products, exclusive offers, and creative uses of our 3D printed accessories.
              </p>
              
              <form className="flex flex-col sm:flex-row gap-4 max-w-md mx-auto" onSubmit={(e) => e.preventDefault()}>
                <Input
                  type="email"
                  placeholder="your.email@example.com"
                  className="flex-1 bg-[var(--muted)] border-[var(--border)] text-[var(--foreground)] placeholder:text-[var(--muted-foreground)]"
                  required
                />
                <Button 
                  type="submit"
                  className="bg-[var(--color-cta)] hover:bg-[var(--color-cta)]/90 text-white px-6"
                >
                  Subscribe
                </Button>
              </form>
              
              <p className="mt-4 text-xs text-[var(--muted-foreground)]">
                No spam, ever. Unsubscribe anytime.
              </p>
            </CardContent>
          </Card>
        </motion.div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>ls -la components/landing/newsletter.tsx && grep -c "Subscribe\|email" components/landing/newsletter.tsx</automated>
  </verify>
  <done>Newsletter section with email input and Subscribe button (UI only)</done>
</task>

<task type="auto">
  <name>Task 5: Update landing page to include all sections</name>
  <files>app/page.tsx</files>
  <action>
Update `app/page.tsx` to include all the remaining sections:

```tsx
import { Hero } from "@/components/landing/hero";
import { FeaturedProducts } from "@/components/landing/featured-products";
import { Benefits } from "@/components/landing/benefits";
import { HowItWorks } from "@/components/landing/how-it-works";
import { UsageScenarios } from "@/components/landing/usage-scenarios";
import { Testimonials } from "@/components/landing/testimonials";
import { Newsletter } from "@/components/landing/newsletter";
import { Navbar } from "@/components/layout/navbar";
import { Footer } from "@/components/layout/footer";

export default function Home() {
  return (
    <div className="min-h-screen flex flex-col bg-[var(--background)]">
      <Navbar />
      <main className="flex-1">
        <Hero />
        <FeaturedProducts />
        <Benefits />
        <HowItWorks />
        <UsageScenarios />
        <Testimonials />
        <Newsletter />
      </main>
      <Footer />
    </div>
  );
}
```

Now the landing page has ALL sections in order:
1. Hero
2. Featured Products
3. Benefits
4. How It Works
5. Usage Scenarios
6. Testimonials
7. Newsletter
8. Footer
  </action>
  <verify>
    <automated>grep -c "HowItWorks\|UsageScenarios\|Testimonials\|Newsletter" app/page.tsx</automated>
  </verify>
  <done>Landing page at app/page.tsx now includes all 7 sections plus Navbar and Footer</done>
</task>

</tasks>

<verification>
- How It Works section has 3 steps with icons and descriptions
- Usage Scenarios section has 4 cards: Desk Work, Travel, Video Calls, Gaming
- Testimonials section has 3 mock reviews with avatars and star ratings
- Newsletter section has email input and subscribe button
- All sections use brand colors and framer-motion animations
- Landing page complete with all sections from LAND-01 through LAND-07
</verification>

<success_criteria>
- User sees "How It Works" section with 3 steps (Unfold, Adjust, Enjoy)
- User sees "Perfect For Every Situation" section with 4 scenario cards
- User sees "What Our Customers Say" section with 3 testimonials
- User sees "Stay in the Loop" newsletter signup with email field
- All sections animate on scroll reveal
- Newsletter form has email validation (UI only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-landing-page/01-03-SUMMARY.md`
</output>